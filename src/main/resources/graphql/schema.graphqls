schema {
    query: Query
    mutation: Mutation
}
type Mutation {
    register(form: UserRegistrationForm): User
    login(form: UserLoginForm): JWTToken
}

type Query{
    getUserById(id:ID):User
}
